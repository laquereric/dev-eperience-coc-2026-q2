<div>
  <h2 class="text-xl font-bold mb-4">Actors</h2>
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
    <% actors.each do |actor| %>
      <%= render DevExperience::ActorCard::Component.new(actor: actor) %>
    <% end %>
  </div>
</div>
